# Supabase (Phase 0 - Foundation)
# Get these from: Supabase Dashboard > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# API URL (optional)
# NEXT_PUBLIC_API_URL: when set, browser uploads go to this backend (recommended for OCR/processing).
# BACKEND_API_URL: when uploads go to Next.js, Next.js calls this to trigger processing (metadata + OCR).
# Example: http://localhost:3000
# NEXT_PUBLIC_API_URL=http://localhost:3000
# BACKEND_API_URL=http://localhost:3000
