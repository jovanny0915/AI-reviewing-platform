# Supabase (Phase 0 - Foundation + Auth)
# Get these from: Supabase Dashboard > Project Settings > API
# Enable Email auth and "Confirm email" (OTP) in Authentication > Providers > Email
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# API URL (optional)
# For local dev with backend: set BACKEND_API_URL so Next.js proxies /api/* to the backend.
# The frontend uses same-origin by default, so all API calls go through this app (Next.js routes or proxy).
# NEXT_PUBLIC_API_URL: override in browser to call backend directly (e.g. for CORS or debugging).
# Example (local): run backend (pnpm run dev:all) and set:
# BACKEND_API_URL=http://localhost:3000
# Production backend:
# BACKEND_API_URL=https://ai-reviewing-platform-backend.vercel.app
